- if title?
  div class="cp-paragraph-title"
    em =title

- case (role)
- when 'text-left'
    - confluence_alignment = 'text-align: left;'
- when 'text-right'
    - confluence_alignment = 'text-align: right;'
- when 'text-center'
    - confluence_alignment = 'text-align: center;'
- when 'text-justify'
    - confluence_alignment = 'text-align: justify;'

- content_without_soft_line_breaks = remove_soft_line_breaks(content)
- if confluence_alignment
  *{tag: %(p), style: "#{confluence_alignment}"}  =content_without_soft_line_breaks
- else
  *{tag: %(p)} =content_without_soft_line_breaks
